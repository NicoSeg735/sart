'use client'

export default function FormContainer() {
  // const userId = 4
  // const { data, isLoading, error } = useQuery({
  //   queryKey: ['user', userId],
  //   queryFn: () => getUser(userId)
  // })

  // console.log('desde el componente', data.data)

  return <span>Formulario</span>
}
